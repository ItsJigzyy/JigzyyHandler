const { MessageEmbed } = require("discord.js");
const fetch = require('node-fetch');

module.exports = {
    name: 'Trivia',
    aliases: ['triv'],
    category: 'Fun',
    utilisation: '{prefix}triv',
    async execute(client, message, args) {

      
    }
}